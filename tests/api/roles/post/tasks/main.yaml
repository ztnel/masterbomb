---
- name: Test /suppliers/add
  uri:
    url: "{{ api_base_url }}/suppliers/add"
    method: POST 
    headers:
      accept: application/json
    status_code: "{{ status_code }}"
    body:
      name: Test Supplier Name
      website: www.test-supplier.com
    body_format: json
  register: post_response

- name: Test /projects/add
  uri:
    url: "{{ api_base_url }}/projects/add"
    method: POST 
    headers:
      accept: application/json
    status_code: "{{ status_code }}"
    body:
      name: Test Project Name
      description: This is a test description
    body_format: json
  register: post_response

- name: Test /manufacturers/add
  uri:
    url: "{{ api_base_url }}/manufacturers/add"
    method: POST 
    headers:
      accept: application/json
    status_code: "{{ status_code }}"
    body:
      name: Test Manufacturer 
    body_format: json
  register: post_response

- name: Test /parts/add
  uri:
    url: "{{ api_base_url }}/parts/add"
    method: POST 
    headers:
      accept: application/json
    status_code: "{{ status_code }}"
    body:
      name: TIP22
      description: Sample part
      manufacturer_id:
      supplier_id: 2
      unit_price: 4.30
    body_format: json
  register: post_response